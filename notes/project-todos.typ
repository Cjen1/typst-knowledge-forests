#import "../site.typ": *
#kt-note(id: "project-todos", title: "Project TODOs", tags: ("planning", "todo"), _ => [

- Remove generated `transclusions.typ` by moving transclusion resolution into Typst runtime registration/loading.
- Implement Typst AST link extraction.
- Finalize graph model and diagnostics.
- Complete artifact generation and step isolation hardening.
- Add/maintain progress output polish.
- Wire/maintain make wrapper integration.
- Validate recursive note discovery.
- Add author metadata support.
- Add a low-priority local serve endpoint command.
])
