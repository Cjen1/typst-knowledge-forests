#import "../site.typ": *
#kt-note(id: "backlinks", title: "Backlinks", tags: ("graph", "prebuild"), transclude => [

Backlinks are generated before rendering from outgoing note references.

#transclude("typst-machinery", mode: "inline")
])
